## UTOP reminders

```ocaml

#require "CS51Utils" ;;
(* To load a module required by a file. *)

#use "lab1.ml" ;;
(* To open and run functions in an OCaml file.  *)

#mod_use "nativeLazyStreams.ml";;
(* To use functions declared in a different module. *)
```
